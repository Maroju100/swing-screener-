__pycache__/
*.pyc
.claude/worktrees/

# Backtest harness working directory (scripts/margin_style_17h_backtest.py and
# friends). Regenerated deterministically on every run: checked-out copies of past
# engine revisions plus temp hist/quotes/actions files. NOT evidence - the evidence
# Rule 2 requires is the committed scripts in scripts/ and data in data/, which
# reproduce this directory. It also holds sandbox state.json files that must never
# be mistaken for docs/margin_style_live_state.json, the real-money state.
.backtest_scratch/
